import time 
from selenium import webdriver

def PercentageSaved():
    # Will read html elements from the product page and return the percentage saved
    #sale_percentage = driver.find_element(By.CLASS_NAME, 'save-percentage')
